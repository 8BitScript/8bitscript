---
"@8bitscript/cli": minor
"8bitscript-lang": patch
---

Doctor offers every emulator it can install by default (`--want` to narrow, `--install` to skip the prompt). VS Code groups machines by family and has a Doctor panel (`8bitscript.doctorEmulators`, default all) with Install selected.
