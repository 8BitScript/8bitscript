---
"@8bitscript/cx16": patch
---

`8bs run cx16` no longer starts x16emu with the mouse and keyboard captured: the pointer moves in and out of the emulator's window like any other, which is how Studio sits beside the editor; ⇧⌘M (Ctrl+M on Linux/Windows) captures on request.
