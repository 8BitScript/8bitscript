---
"@8bitscript/cx16": patch
---

`8bs run cx16` starts x16emu captured again: uncaptured, the KERNAL mouse gets relative motion and drifts from the host pointer. ⇧⌘M (Ctrl+M on Linux/Windows) releases the mouse to leave the window.
