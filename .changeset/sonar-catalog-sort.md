---
"@8bitscript/compiler": patch
"@8bitscript/cli": patch
---

Catalog locale lists sort with `localeCompare`, unused `schemaKeys` is gone, and IntelliSense no longer uses a shared object literal as a default parameter. Same behaviour; the patterns SonarCloud's Reliability gate scores are not.
