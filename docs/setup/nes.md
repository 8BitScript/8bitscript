---
title: FCEUX
nav_order: 23
---

# FCEUX

The NES target launches in FCEUX. Headless screenshots use its Lua
hook (`gui.savescreenshotas`), not a CLI still-frame flag.

```
brew install fceux
sudo apt-get install -y fceux
sudo pacman -S fceux
```
