// A file with deliberate errors, used to check that diagnostics reach the
// terminal and the editor. `8bs check` must report every line below.
let score: utinyint = 300;
let health: tinyint = -200;
let name = "unterminated;
import { a } from "@8bitscript/nonexistent";
import { b } from "markdown-it";
import { c } from "./missing.8bs";
