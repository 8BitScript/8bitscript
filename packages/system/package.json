{
  "name": "@8bitscript/system",
  "private": true,
  "version": "0.0.0",
  "description": "Which machine is being built: one name per system, and System.CURRENT for the one this build is for. A compile-time constant, so a program can branch on the machine without a per-machine file.",
  "license": "MIT",
  "8bitscript": {
    "entry": "./src/index.8bs"
  },
  "files": [
    "src"
  ],
  "scripts": {
    "test": "node --test"
  },
  "publishConfig": {
    "access": "public"
  }
}
