// @8bitscript/system — this build is for the Atari 8-bit family. Read by index.8bs
// through the filename rule; the number is System.ATARI8's, and
// test/system.test.mjs holds the two in step.
export namespace Current {
    const SYSTEM: utinyint = 5;
}
