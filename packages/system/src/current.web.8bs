// @8bitscript/system — this build is for the browser runtime. Read by index.8bs
// through the filename rule; the number is System.WEB's, and
// test/system.test.mjs holds the two in step.
export namespace Current {
    const SYSTEM: utinyint = 0;
}
