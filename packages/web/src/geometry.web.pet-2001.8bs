// @8bitscript/web — 40×25 PET 2001 skin. Picked up when the build's
// hardware tag is `pet-2001`. Facts come from @8bitscript/pet (40×25, no
// per-cell color); the host paints green-on-black. See geometry.8bs.

export namespace Video {
    const COLUMNS: utinyint = 40;
    const ROWS: utinyint = 25;
    const CELL_COUNT: usmallint = 1000;
    const CHAR_BASE: usmallint = 2;
    const COLOR_BASE: usmallint = 1002;
    const INPUT_OFFSET: usmallint = 2002;
    const HOST_OFFSET: usmallint = 2003;

    // The same three questions the Modern host answers at run time, answered
    // here from constants: this skin is a machine, and a machine's grid does
    // not change. Both fold, so a program that asks costs nothing for asking.
    const RESIZABLE: bool = false;

    function columns(): utinyint {
        return Video.COLUMNS;
    }

    function rows(): utinyint {
        return Video.ROWS;
    }

    function cells(): usmallint {
        return Video.CELL_COUNT;
    }
}
