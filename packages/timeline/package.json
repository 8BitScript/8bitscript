{
  "name": "@8bitscript/timeline",
  "version": "0.22.0",
  "description": "Frame-counted cues for 8BitScript programs — at frame 120 the logo drops, at 300 the bars start — the same pure code on every target: a counter the program ticks once a frame and predicates (at, after, between, every) that fold against literal frame numbers, so a showcase is a sequence rather than a loop.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/8BitScript/8bitscript.git",
    "directory": "packages/timeline"
  },
  "8bitscript": {
    "entry": "./src/index.8bs"
  },
  "files": [
    "src"
  ],
  "scripts": {
    "test": "node --test"
  },
  "publishConfig": {
    "access": "public"
  },
  "devDependencies": {
    "@8bitscript/screen": "workspace:*",
    "@8bitscript/text": "workspace:*"
  }
}
