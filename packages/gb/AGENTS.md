# Writing Game Boy support

This file is for anyone — human or agent — touching `packages/gb`.
Read the root `AGENTS.md` first.

> **Game Boy is its own machine.** Do not translate register numbers
> from the C64, NES, or any other package. Facts come from this catalog
> and from the research page, not from a sibling target.

CPU family: **sm83**. Image: `.gb`.
Emulator: `sameboy` (dedicated).
