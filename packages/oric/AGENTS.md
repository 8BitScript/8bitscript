# Writing Oric-1 / Atmos support

This file is for anyone — human or agent — touching `packages/oric`.
Read the root `AGENTS.md` first.

> **Oric-1 / Atmos is its own machine.** Do not translate register numbers
> from the C64, NES, or any other package. Facts come from this catalog
> and from the research page, not from a sibling target.

CPU family: **mos**. Image: `.tap`.
Emulator: `mame` (mame).
