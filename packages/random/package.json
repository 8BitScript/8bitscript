{
  "name": "@8bitscript/random",
  "version": "0.18.0",
  "description": "Deterministic pseudo-random generators, explicitly seeded, the same code on every target: a 16-bit LCG at the bare import, a precomputed lookup table at ./table. Hardware entropy stays opt-in: ./entropy reads SID voice 3 on the C64 and POKEY on the Atari 8-bit, and is the LCG everywhere else.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/8BitScript/8bitscript.git",
    "directory": "packages/random"
  },
  "8bitscript": {
    "entry": "./src/index.8bs",
    "exports": {
      "./table": "./src/table.8bs",
      "./entropy": "./src/entropy.8bs"
    }
  },
  "files": [
    "src"
  ],
  "dependencies": {
    "@8bitscript/atari8": "workspace:*",
    "@8bitscript/c64": "workspace:*"
  },
  "scripts": {
    "test": "node --test"
  },
  "publishConfig": {
    "access": "public"
  }
}
